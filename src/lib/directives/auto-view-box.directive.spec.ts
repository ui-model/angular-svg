import { AutoViewBoxDirective } from './auto-view-box.directive';

describe('AutoViewBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoViewBoxDirective();
    expect(directive).toBeTruthy();
  });
});
